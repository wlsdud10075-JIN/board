# board 알림톡 템플릿 초안 (BizM 등록용)

> BizM 콘솔에 **아래 문구 그대로** 등록·승인받는다. 발송 msg 가 승인본과 글자까지 같아야 통과.
> 코드 정본 = `app/Support/AlimtalkTemplates.php`. 문구 수정 시 **여기 + 코드 + BizM 동시** 반영.
> 발신프로필 = **car-erp 와 공유**(같은 Bizm 계정, Jin 2026-07-12).

> 📌 **2026-07-27 1차 반려 → 문구 개정(아래가 재검수본)**. 반려 사유 = "수신 대상이 불명확 —
> 사내 임직원 대상인지, 어떤 상황·액션으로 발송되는지 밝힐 것". 원인 = **"회원님"** 호칭이 B2C
> 가입자 안내처럼 읽힘(car-erp 14종은 호칭 없이 "ERP에서 ~해 주세요" 업무지시형이라 통과).
> 개정점 3가지: ① 맨 앞 `[사내 업무용]` ② "회원님" → **"담당자님"** ③ `ssancar.com`/`board` →
> **"사내 업무 시스템(board)"**. 템플릿코드·템플릿명·프로필·카테고리(008002)·유형(BA)·변수는 불변.

## board_region_inspection — 지역 검차 안내
- 수신자: 그 지역×날짜 배정 검차원(`InspectionAssignment`).
- 변수: `#{지역}` · `#{건수}` · `#{차량목록}`(가변, 개행 구분 차량번호 목록 — 길이 검수 주의).
- 유형: 기본형(선택안함). title 미사용.

```
[사내 업무용] 검차 배정 안내 #{지역}

담당자님, 배정된 지역에 검차가 접수되어 안내드립니다.

■ 지역: #{지역}
■ 검차 대상: #{건수}대
#{차량목록}

사내 업무 시스템(board)에서 검차 결과를 등록해 주세요.
```

예시 렌더(#{차량목록} = 개행 목록):
```
[사내 업무용] 검차 배정 안내 경기 화성

담당자님, 배정된 지역에 검차가 접수되어 안내드립니다.

■ 지역: 경기 화성
■ 검차 대상: 2대
12가3456
34나5678

사내 업무 시스템(board)에서 검차 결과를 등록해 주세요.
```

## board_forward_ready — 사진/영상 완료·전달대기 (→ 영업담당자)
- 수신자: 그 매물 작성 영업(`purchase_listings.created_by_user_id`).
- 트리거: ssancar 자동전이(draft→inspected/전달대기) 직후. 변수: `#{차량번호}`.
- 유형: 기본형. title 미사용.

```
[사내 업무용] 전달 대기 안내 #{차량번호}

담당자님, 등록하신 매물 #{차량번호}의 사진/영상 업로드가 완료되어 바이어 전달 대기 상태입니다.

사내 업무 시스템(board)에서 확인 후 바이어에게 전달해 주세요.
```
